# cloud.md — work diary

One line per step, newest at the bottom. Whoever (or whatever) picks this up next: read the
last few lines, then `CLAUDE.md` for the state and the conventions.

## 2026-09-19

- Cloned the old MediScan repo for reference; found the trained eye checkpoints only on this laptop (`Downloads\MediScan-main\...\backend\weights`). TF 2.21/Keras 3 needed; made `.venv` with tensorflow-cpu 2.21.
- Decision: rebuild from scratch as **Venus AI**, five stages exactly as the SIH26038 architecture PDF; Python/FastAPI + React reference implementation, MATLAB port alongside.
- Wrote Stage 0 (gate/FOV/quality/enhance), Stage 1 (classical OD/fovea/vessels/lesions), Stage 2 (v1 grader + ICDR rule grader + fusion), Stage 3 (Grad-CAM, overlays, attention agreement, PDF), Stage 4 (district DES + sweep), Stage 5 (tiers, allocator, SQLite), FastAPI, React front end (5 screens), 29 tests.
- Locked the first operating point from the v1 grader's frozen EyePACS scores (patient-disjoint halves, Platt, 90 % sens threshold): sens 0.911 / spec 0.607 / AUC 0.883.
- Tuned the classical lesion detectors on the 8 shipped samples (robust MAD thresholds, yellowness test, moment elongation); compiled Grad-CAM with tf.function (2.7 s → 80 ms).
- Timing on the laptop CPU: median 3.3 s, p95 4.2 s per image. Pushed everything to github.com/abhay1074/Venus.

## 2026-09-20

- User: 9 days left, make it complete (not a demo); datasets are on disk in the MediScan folder; MATLAB will be installed; GPU (RTX 5060, WSL) free.
- Verified WSL GPU works only with the pip-CUDA `LD_LIBRARY_PATH` + PTX cache → `scripts/wsl-gpu.sh`. Found XLA + mixed precision gives 130 img/s for B3@512 (vs 14 fp32).
- Wrote the data plan: `backend/data/sources.py` (APTOS 3,662 / EyePACS 35,126 / DDR 13,673 + 757 lesion masks), `cache_stage0.py` (512-px cache, 52,461 images, 0 errors, ~50 min), `rehash.py` (256-bit pHash; nearest-neighbour distances bimodal → threshold 10), `build_manifests.py` (290 near-dups dropped; train 39,147 / val 3,403 / calibration 2,000 EyePACS patients / **external DDR test 3,720** / held-out EyePACS 2,758 / ungradable 1,142; fingerprints).
- Wrote trainers: `train_grader.py` (EfficientNet-B3@512, ordinal head, XLA+AMP), `train_lesion_unet.py` (DDR masks), `train_quality.py` (EyeQ labels, 12,543 EyePACS images). Shared nets in `backend/venus/nets.py`. Serving now selects grader v2 when `backend/weights/grader_v2.weights.h5` exists; U-Net and quality CNN are optional with classical/handcrafted fallbacks.
- Launched `scripts/train-all.sh` on the GPU (grader 15 epochs → scoring → U-Net 60 epochs → quality 12 epochs). **Epoch 1 val referable-AUC 0.943, QWK 0.79** (v1: 0.887 / 0.46); ~12 min/epoch.
- Wrote the MATLAB implementation: `matlab/+drscreen` (all stages), `matlab/simulink` (MATLAB DES reference, SimEvents builder, parsim sweep, Pareto plot), `matlab/app/DRScreenApp.m`, `matlab/tests` (4 test classes), `matlab/README.md`. Not yet executed anywhere — MATLAB is not installed; installing Octave (winget) for a syntax pass.
- Added CI (`.github/workflows/ci.yml`), Dockerfiles + compose, `scripts/pull-models.sh`, `backend/eval/write_docs.py` (VALIDATION.md + model cards generated from JSON), `export_models.py` (SavedModel/ONNX for MATLAB). Front-end Validation page updated to the new operating-point schema. Pushed.
- Now: waiting for training (grader done ≈ 3 h after 11:40 UTC WSL time), Octave installing in the background; next is pulling the weights, calibrating v2 (`backend.eval.calibrate --tag grader_v2 --model-version venus-dr-2.0.0`), re-timing, re-running the sweep, regenerating docs, pushing.
- Installed GNU Octave 11.3 (winget) as a stand-in parser/runtime: all 30 non-test MATLAB files parse; `matlab/octave_smoke.m` executes gradeRule / fuse / calibrate / tier / simulateDistrict with the MATLAB tests' cases — 0 failures after fixing two MATLAB-invalid idioms Octave caught (indexing into a function-call result; two-argument `round`). Test classes need `matlab.unittest`, so those still wait for MATLAB.
- Dry-ran `train_lesion_unet.py` and `train_quality.py` on the Windows CPU with tiny subsets (via `\wsl$`): both run end to end; fixed a class-count guard in the quality trainer. Grader v2 epoch 3 val AUC 0.957; UI now shows grader tag, lesion method (U-Net/classical) and quality-CNN probabilities; `scripts/finalise-models.sh` written (pull weights → calibrate v2 → export → docs).
- Wrote `backend/eval/flag_rate.py` (validation sample through the served Stage 0–3 path: flag rate + reasons, attention agreement for correct vs incorrect referable calls, rule grader alone); the district simulation now defaults to the measured flag/retake rates from `config/validation_flags.json`. Appended it to `train-all.sh` (runs after the quality CNN, after pulling weights). Architecture doc updated for the model track.
- Offline deployment: the API now serves `frontend/dist` at `/` when it exists (one process, no Node, no network on the PHC laptop); `scripts/build-offline-bundle.ps1` produces a folder with a wheel cache and `run.bat`. Verified the static site is served from :8000.
- Grader v2 training finished (15 epochs, 138 min): best validation referable-AUC **0.9636** at epoch 8, plateau ~0.962 from epoch 6. Chain continues: scoring → U-Net → quality CNN → calibrate v2 → flag rate → export → docs.
- Chain done: grader v2 val AUC 0.964; U-Net (re-scored with full-pixel AUPR after finding the subsampled metric inflated: DDR test HE 0.45 / EX 0.48 / SE 0.26 / MA 0.08, Dice 0.46/0.49/0.31/0.17); quality CNN held-out AUC 0.992. Locked the **v2 operating point**: external DDR test (3,720, once) **AUC 0.975 [0.970–0.979], sens 0.947, spec 0.889, PPV@18% 0.651, ECE 0.042** — all PS targets met; held-out EyePACS AUC 0.954.
- Found and fixed two integration problems with the validation flag-rate run: (1) the U-Net on out-of-distribution colours produced false hemorrhages → LAB colour normalisation to DDR stats on the un-enhanced frame; (2) the EyeQ quality CNN as a gate refused 46% of images / 78% of PDR → it now decides good/usable and warns; hard limits reject. 29 tests pass with all models active. Flag-rate analysis re-running.
- User rule (standing): no hardcoded or precomputed demo results anywhere in the source — everything shown runs live. `docs/DEMO.md` is a presenter script only; the only persisted artefacts are the locked operating point and the sweep cache the server recomputes itself.
- Messidor-2 arrived (4-part zip + ADCIS pairing CSV); fetched the Krause 2018 adjudicated grades via the Kaggle CLI; `backend/data/freeze_external.py` cached, dedup-checked (0 near-duplicates) and fingerprinted it (1,744 gradable); `backend/eval/score_external.py` scored it once at the locked threshold: **AUC 0.963, sens 0.980, spec 0.645, ECE 0.08** (its own ROC: 0.90/0.90 → calibration shift across sources, over-refers).
- Flag-rate analysis (final rules, 498 val images): flag rate 25.1%, retake 2.2%, attention agreement median 0.54 (correct) vs 0.27 (incorrect), rule grader alone sens 0.91 / spec 0.49. CPU timing with all networks: median 4.2 s, p95 5.0 s. Sweep re-run with v2: **2 ophthalmologists with AI vs 7 without, ₹0.69 Cr vs ₹1.72 Cr**. docs/VALIDATION.md regenerated; README numbers refreshed.
- End of day 1 (20 Sep): repo clean (a stray copy of the Messidor zip parts had been swept into a commit; removed before push, archives now gitignored). Preflight passes with grader_v2 + U-Net + quality CNN; PDR sample screens live in 4.2 s: grade 4 / rule 4 / P(NV) 1.00 / P1. Tomorrow: MATLAB verification with the user's install, then the second model round if warranted (see the plan in the chat log: longer schedule + TTA on calibration; U-Net patch training if MA stays weak; attention flag threshold from validation).
- Left-overs of day 1 done: networks exported for MATLAB (`models/export`, 213 MB, gitignored); GPU timing run (median 3.4 s — inference is not the cost); found Stage 3's PNG `optimize=True` was ~1.5 s of every result → fast lossless PNG: **CPU end-to-end median 4.2 s → 1.7 s**; all five screens re-checked live with v2 (PDR sample: grade 4/4, P(NV) 1.00, P1 booked at the district hospital). 29 tests pass.
- Evening of day 1, tomorrow's prerequisites: **review policy chosen on validation** (`backend/eval/review_policy.py` → `config/review_policy.json`): abstain band ±0.35 in logit space (the architecture's ±0.05 probability is asymmetric at a 0.106 threshold), attention floor 0.20 (highest cut where ≥60 % of flagged calls are CNN errors); resulting flag rate 27.7 %, error rate 31.9 % among flagged vs 11.4 % unflagged, 51.8 % of CNN errors flagged. Wired into fusion (`abstain_low/high`), Stage 3, the simulation's flag rate and the ProbabilityBar. TTA on the calibration set: AUC 0.9466 → 0.9493 (not enabled for CPU serving; the GPU launcher may).
- Second model round prepared and **launched overnight** (`scripts/train-night.sh` via `scripts/launch-night.ps1`, log `~/venus-cache/train-night.log`): (1) DDR lesion cache at 1024 px (`cache_stage0 --size 1024`); (2) `train_lesion_unet --size 1024 --patch 512 --repeats 4 --epochs 80` — lesion-biased random crops, index-shuffled tf.data (no frame copies), bit-packed masks, streaming 8,192-bin histogram AUPR (verified equal to sklearn to 4 dp), full-frame evaluation through a second graph sharing the weights; tag `lesion_unet_v2_1024`, thresholds written next to the weights (only the default tag touches `config/lesion_thresholds.json`); (3) grader seed 7 (`--seed`) scored on calibration + val only, for an ensemble decision in the morning. Serving already handles a larger U-Net frame: `stage1.unet_lesions` re-normalises the raw upload at the network's `frame_size` and averages the probabilities back to 512. Nothing here touches the external tests or the served checkpoints. 29 tests pass.
- Morning of day 2 (21 Sep), first: read `~/venus-cache/train-night.log`; compare `lesion_unet_v2_1024.thresholds.json` (valid AUPR, esp. MA) with the served 0.08/0.45/0.48/0.26; if better, copy weights + thresholds via `scripts/finalise-models.sh` style promotion and re-run `flag_rate` + `review_policy` + `write_docs`. For the grader, average seed-42 and seed-7 calibration predictions (`~/venus-cache/predictions/`) and compare AUC before deciding on an ensemble (that would need a `calibrate` ensemble mode and a `grader_tag` bump, then the external test scored once more under a new model version).
- `backend/eval/compare_graders.py`: AUC (+ bootstrap CI) per grader tag and for the mean of their P(≥k), paired bootstrap difference vs the best single, on calibration/val only (external manifests refused). Morning command: `wsl bash scripts/wsl-gpu.sh backend.eval.compare_graders --tags grader_v2 grader_v2_s7`. Night chain confirmed alive: U-Net v2 epoch-5 checkpoint written at 18:16, GPU 76–84 %, 4.7 GB; ~1.5 min/epoch → U-Net done ≈ 20:30, grader seed 7 ≈ 23:00.
- Day 2 (21 Sep), morning. The laptop was shut down (overheating) during epoch 15/15 of grader seed 7; its best-on-val checkpoint (epoch 8, val AUC 0.965) had been saved, so nothing was retrained. `scripts/train-day2.sh` (launched with `launch-night.ps1 -Script train-day2.sh`) scored it and ran `compare_graders`: **ensemble = +0.003 AUC on calibration (0.947 → 0.950, paired CI [−0.0004, 0.0067]), +0.002 on val; equal to TTA, and TTA on top adds nothing** → not shipped (would double CPU grader cost, cost a new model version and a second external-test scoring). Recorded in `config/ensemble_check.json` and a "tried and not shipped" section of docs/VALIDATION.md. The chain then started a longer 1024-px U-Net (`lesion_unet_v3_1024`, 160 epochs, ~1.5 h) because v2's valid curve was still rising at 80.
- MATLAB port mirrors the review policy: `+drscreen/reviewPolicy.m`, `fuse.m` (logit band, `abstainLow/High`), `explain.m` (attention floor), `loadModels` carries `models.policy`; GradeTest + octave_smoke cover it (Octave: 0 failures).
- Sweep re-run at the served flag rate (27.7 %): slide numbers unchanged (2 ophthalmologists with AI vs 7; ₹0.69 Cr vs ₹1.72 Cr). `GET /validation-extras` serves review_policy / validation_flags (without rows) / lesion_thresholds / ensemble_check; the Validation screen shows three new cards from it (review policy, lesion U-Net test numbers, "measured and not shipped"). Frontend builds; 29 tests pass. GitHub push blocked from this network (HTTPS to everything fails with an untrusted root — captive portal or TLS interception); commits are local until the network is back.
